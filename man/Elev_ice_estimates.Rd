% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{Elev_ice_estimates}
\alias{Elev_ice_estimates}
\title{Lake elevation and ice phenology estimates}
\format{
A data frame with one row per waterbody and the following columns:
\describe{
  \item{WBID}{Character. Unique waterbody identifier (leading zeros preserved).}
  \item{elevation_est}{Numeric. Best-available surface elevation in metres: the
    DataPond \code{elevation_m} where present, otherwise the estimate from
    \code{find_elevation()}. Drives the ice estimate below.}
  \item{ice_on_day}{Integer. Estimated typical ice-on day of year, or \code{NA}
    for lakes that do not typically freeze (from \code{estimate_lake_ice()}).}
  \item{ice_off_day}{Integer. Estimated typical ice-off day of year (from
    \code{estimate_lake_ice()}).}
}
}
\source{
Built by \code{data-raw/Create_Elev_ice_estimates.R} from DataPond waterbody
  views, with elevations from \code{find_elevation()} (elevatr) and ice timing
  from \code{estimate_lake_ice()}.
}
\usage{
data(Elev_ice_estimates)
}
\description{
One row per lake (WBID) giving a best-available surface elevation and the
estimated typical ice-on / ice-off timing. Built once (e.g. annually) so the
slow remote elevation lookup in \code{find_elevation()} does not have to run
on every \code{Lakes} build; \code{Create_Lakes.R} joins this table in and
fills any lakes that are missing a DataPond elevation. All values are
estimates/derived, not direct observations.
}
\examples{
data(Elev_ice_estimates)
head(Elev_ice_estimates)
}
\keyword{datasets}
