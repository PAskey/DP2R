% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{FISS_Spp}
\alias{FISS_Spp}
\title{Known FISS fish observations by lake}
\format{
A data frame with one row per distinct observation and the following columns:
\describe{
  \item{WBID}{Character. Waterbody identifier, matching the DataPond WBID
    (the provincial waterbody_identifier).}
  \item{species_code}{Character. FISS species code as published in the catalogue.}
  \item{year}{Integer. Calendar year extracted from OBSERVATION_DATE.}
  \item{activity}{Character. Activity / sampling method of the observation
    (FISS ACTIVITY field).}
  \item{agency_name}{Character. Agency that reported the observation.}
}
}
\source{
Generated by \code{data-raw/Create_FISS_Spp.R} from the bcdata R
  package (BC Data Catalogue: Known BC Fish Observations and BC Fish
  Distributions).
}
\usage{
data(FISS_Spp)
}
\description{
A raw, per-observation table of known fish drawn from the provincial FISS
"Known BC Fish Observations" point layer (POINT_TYPE_CODE == "Observation"
only), restricted to lakes and species DP2R knows about. Kept unsummarised so
it can be summarised different ways later; \code{linkClips()} derives its own
first/last observation year from it for \code{Lake_Spp}. Built once (e.g.
annually) rather than re-querying the BC Data Catalogue.
}
\examples{
data(FISS_Spp)
head(FISS_Spp)
}
\keyword{datasets}
